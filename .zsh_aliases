# Add aliases here

# https://gist.github.com/arch-raven/d3e8e8898274dfab0e728d17fdd4d05a
alias labArnab='ssh -N -f -L localhost:7878:localhost:7878 19144026@10.2.60.42'

# Default preferences
alias vim="nvim"

# Kitty
if [[ "$TERM" == "xterm-kitty" ]]; then
    alias ssh="kitty +kitten ssh"
    alias icat="kitty +kitten icat"
    alias diff="kitty +kitten diff"
    alias clipboard="kitty +kitten clipboard"
    alias hgrep="kitty +kitten hyperlinked_grep"
fi

# ls aliases
alias lsh="ls -A --color=auto --hyperlink=auto"
alias lse="exa -a --color=always --group-directories-first --icons"
